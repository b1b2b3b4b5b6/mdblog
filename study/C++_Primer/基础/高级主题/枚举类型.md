# 枚举类型
```cpp
enum class color : int;
```
* 可以进行前置声明, 但必须指定成员大小

## 不限定作用域的枚举类型
```cpp
enum color:int {
  red,
  yellow,
  green
}
```
* 枚举成员自动转换成整型

### 限定作用域的枚举类型
```cpp
enum class color: long{
  red,
  yellow,
  green
}
```
* 不会进行到整型的隐式转换